# FINAL CHECKLIST

## ✅ Капча
- 100 вопросов загружаются из `captcha_questions.json`.
- У каждого вопроса 3 варианта ответа.
- Кнопки капчи остаются вертикальными.
- Таймер проверки: 120 секунд.
- Неправильный ответ меняет вопрос и не создаёт новый общий таймер.
- Приветствие после успешной проверки сохранено без изменений.

## ✅ Модерация
- Обычный мат без адресата не наказывается.
- Адресованное оскорбление учитывается в окне 10 минут.
- 1 нарушение: предупреждение.
- 2 нарушение: строгое предупреждение.
- 3 нарушение: мут на 1 минуту.
- После мута счётчик сбрасывается.
- Старые нарушения автоматически очищаются.
- За мат и оскорбления не используется ban/kick.

## ✅ Анекдоты
- `jokes.json` валиден.
- Пустых строк нет.
- Точных дублей нет.
- Повреждённой кодировки `?????` нет.
- Кулдаун `/анекдот`: 60 секунд отдельно на пользователя.
- Последние 500 анекдотов исключаются из повторного выбора.
- Пустая база обработана без падения.

## ✅ Новые команды v2.0
- `/история`: база `stories.json`, 1000 историй, кулдаун 60 секунд.
- `/тост`: база `toasts.json`, 1000 тостов, кулдаун 60 секунд.
- `/предсказание`: база `predictions.json`, 1000 предсказаний, кулдаун 60 секунд.
- `/фарт`: база `luck_comments.json`, 200 комментариев, кулдаун 60 секунд.
- Все новые JSON загружаются один раз при импорте модулей.
- У всех новых команд индивидуальный кулдаун по пользователю.
- Для новых команд работает защита от повторов.

## ✅ Ответы
- Архитектура `police_replies.json` сохранена.
- Категории сохранены: `call`, `hello`, `how_are_you`, `what_doing`, `thanks`, `who_are_you`, `sleep`, `bored`, `goodbye`, `fallback`, `frequent`.
- Michel использует `owner_replies.json`.
- Юра использует `yura_replies.json`.
- История повторов хранится отдельно по ключам и очищается.
- Для обычных ответов используется отдельная история по пользователю и категории.

## ✅ Режим "Молчаливый полицейский"
- Police не отвечает на обычные сообщения игроков.
- Police отвечает только на команды, `Police`, username бота или reply на сообщение бота.
- Слово `бот` без явного обращения больше не запускает разговорный ответ.
- Обычный мат без адресата не вызывает случайных реплик.
- 1000 обычных сообщений проверяются тестом без единого ответа conversation-модуля.

## ✅ Render
- `requirements.txt` содержит только лёгкие зависимости.
- `Procfile`: `worker: python main.py`.
- `render.yaml` запускает `python main.py`.
- `runtime.txt` сохранён.
- Токен берётся из переменной окружения `BOT_TOKEN`.
- В файлах проекта токен не записан.

## ✅ JSON
- Все JSON-файлы проходят загрузку.
- Большие JSON загружаются один раз при старте модулей.
- Повреждённых строк `?????` нет.

## ✅ Tests
- Выполнено `python -m compileall .`.
- Выполнено `python -m unittest discover -s tests -v`.
- Все тесты прошли.

## ✅ Release
- Архив собирается без `__pycache__`.
- В архив включены тесты, документация и Render-файлы.
- Новые команды не добавлялись.
- Известное ограничение: база не раздута до 3000 анекдотов и 3000+ ответов мусорными шаблонами.
